# .env.example
# Copy this file to .env and fill in your values
# Make sure to keep .env secure and never commit it to version control

#
# Git Configuration
# These are used by the setup.sh script to configure git globally
#
export GIT_EMAIL="your.email@domain.com"    # Your git commit email
export GIT_NAME="Your Name"                 # Your git commit name

#
# API Tokens
#

# IPInfo Token (https://ipinfo.io)
# Used by the ipinfo() function to get IP address information
# Sign up at https://ipinfo.io/signup
export IPINFO_TOKEN="ipinfo_token_here"

# Docker Hub Token (https://hub.docker.com/settings/security)
# Used for docker login in docker-login function
# Generate at Docker Hub -> Account Settings -> Security -> New Access Token
export DOCKER_TOKEN="docker_pat_token_here"

# Add any additional environment variables below
# Example:
# export AWS_PROFILE="default"
# export GITHUB_TOKEN="your_github_token"
# export KUBECONFIG="~/.kube/config"