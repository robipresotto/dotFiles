# Kubernetes
alias k8a="kubectl apply -f $argv -n $argv"
alias k8dp="kubectl describe $argv -n $argv"
alias k8ci="kubectl cluster-info"
alias k8gpn="kubectl get pods -n $argv"
alias k8gp="kubectl get pod $argv"
alias k8gd="kubectl get deployments"
